Accion hallarProm
D.V.
  Entero n    // Entrada
  Real nota   // Entrada
  Real prom   // Salida
  Real suma
  Entero i
Inicio
  Leer(n)
  i <- 1
  suma <- 0

  Mientras i<=n hacer
    Leer(nota)
    suma <- suma + nota
    i <- i + 1
  FMientras
  prom <- suma/n
  Escribir(prom)
Fin
